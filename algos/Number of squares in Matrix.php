<?php

/**
 * Returns count of rectangles which are formed by 1
 * @param $arr
 */
function rectCount($arr) {
    $count = 0;
    $rows = count($arr);
    $cols = count($arr[0]);

    //TODO implement
    return $count;
}

echo rectCount(9);